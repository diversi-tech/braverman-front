// import { START_TIMER, STOP_TIMER } from '../actions/timerActions';

// const initialState = {
//   currentTimer: null,
//   timeEntries: []
// };

// const timerReducer = (state = initialState, action) => {
//   switch (action.type) {
//     case START_TIMER:
//       return { ...state, currentTimer: action.payload };
//     case STOP_TIMER:
//       return { ...state, currentTimer: null, timeEntries: [...state.timeEntries, action.payload] };
//     default:
//       return state;
//   }
// };

// export default timerReducer;


