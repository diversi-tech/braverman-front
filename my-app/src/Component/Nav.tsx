import { NavLink } from "react-router-dom"

export const Nav = () => {

    return <>
        <div className={'nav'}>
            <NavLink to='' className={'link'} > </NavLink>
        </div>

    </>
}
